Login steps:
1) Open http://<docker-host-ip>:6080
2) In the noVNC desktop, open Firefox and go to https://fusedemo.com/main.aspx
3) Complete SSO login.
4) Close the browser tab.
5) After login is confirmed, switch PLAYWRIGHT_MCP_HEADLESS back to 1 and redeploy.
